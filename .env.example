# CertPath AI does NOT require any server-side environment variables.
# Users provide their own LLM API key via the in-app Settings page,
# which is stored locally in IndexedDB and never sent anywhere except
# the provider's own API endpoint.
#
# This file exists only to document that no secrets are needed to run.
